#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  // input
  int x, a; cin >> x >> a;

  int ans;
  if (x < a) {
    ans = 0;
  } else {
    ans = 10;
  }
  // output
  cout << ans << endl;
}
