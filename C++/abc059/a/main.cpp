#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  // input
  string a, b, c; cin >> a >> b >> c;
  char x = a[0] - 32;
  char y = b[0] - 32;
  char z = c[0] - 32;

  // output
  cout << x << y << z << endl;
}
