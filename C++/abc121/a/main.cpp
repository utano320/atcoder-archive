#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  // input
  int H, W, h, w; cin >> H >> W >> h >> w;

  // output
  cout << (H - h) * (W - w) << endl;
}
