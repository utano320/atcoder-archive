#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  // input
  int m, d; cin >> m >> d;

  // output
  cout << ((m % d == 0) ? "YES" : "NO") << endl;
}
