#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  // input
  char a, b; cin >> a >> b;
  // output
  cout << ((a == b) ? "H" : "D") << endl;
}
