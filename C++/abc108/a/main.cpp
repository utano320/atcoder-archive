#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  // input
  int k; cin >> k;

  // output
  cout << (k / 2) * (k / 2 + k % 2) << endl;
}
