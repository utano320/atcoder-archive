#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  // input
  int a, p; cin >> a >> p;

  int ans = (a * 3 + p) / 2;
  // output
  cout << ans << endl;
}
