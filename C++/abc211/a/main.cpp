#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  double a, b; cin >> a >> b;

  cout << fixed << setprecision(7) << (a + 2 * b) / 3 << endl;
}