#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  // input
  string a, b, c; cin >> a >> b >> c;

  // output
  cout << a[0] << b[0] << c[0] << endl;
}
