#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  // input
  int a; cin >> a;
  string s; cin >> s;

  // output
  cout << ((a >= 3200) ? s : "red") << endl;
}