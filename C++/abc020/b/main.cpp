#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  // input
  string a, b; cin >> a >> b;
  int ans = stoi(a + b) * 2;

  // output
  cout << ans << endl;
}
